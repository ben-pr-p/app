.alert.alert-info
  = t('proposal-options.voted-abstained') + ' '
  - if (neutrals)
    - var voteidx = neutrals.map(function (v) {return v.author}).indexOf(user.id)
    - if (platform)
      = t('proposal-options.voted-by-platform') + ' ' + platform
    = '.'